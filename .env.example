### Database (Postgres)
# Replace with your Postgres connection string. The app reads `DATABASE_URL` via Prisma.
DATABASE_URL="postgresql://postgres:password@localhost:5432/blackoutdb?schema=public"

### NextAuth / App
# Secret used to sign NextAuth JWTs. Generate with: `openssl rand -hex 32`
NEXTAUTH_SECRET="openssl rand -hex 32"
# Public URL of your app (used for links in emails, sitemap, robots, etc.)
NEXTAUTH_URL="http://localhost:3000"

### Docker / Postgres helper vars (optional)
POSTGRES_DB=blackoutdb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Notes:
# - Keep this file out of source control if it contains real secrets.
# - Required env vars (the app will fail fast if missing): DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL, SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM_EMAIL